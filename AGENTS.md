# Repository scope

This repository owns ledger, wallets, reconciliation and financial execution.
It may consume Contracts, Shared, Vault, Node and Rails interfaces, but must not
contain their implementations, deployment manifests or secrets.
