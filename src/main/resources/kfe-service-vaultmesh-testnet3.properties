# Vault Mesh Lab P0 visualization profile. This is not a production go-live profile.
# Import with:
#   --spring.config.additional-location=classpath:kfe-service-vaultmesh-testnet3.properties
# deploy.sh / local-full forces mesh-only=true and base-url=http://vault-1:7701
# (token must match compose VAULT_API_TOKEN, default kerosene-vault-lab-only).

bitcoin.network=testnet3
kfe.vaultmesh.enabled=${KFE_VAULTMESH_ENABLED:true}
kfe.vaultmesh.mesh-only=${KFE_VAULTMESH_MESH_ONLY:false}
kfe.vaultmesh.submit-on-outbound=${KFE_VAULTMESH_SUBMIT_ON_OUTBOUND:true}
kfe.vaultmesh.base-url=${KFE_VAULTMESH_BASE_URL:http://127.0.0.1:7701}
kfe.vaultmesh.connect-timeout-ms=${KFE_VAULTMESH_CONNECT_TIMEOUT_MS:2000}
kfe.vaultmesh.read-timeout-ms=${KFE_VAULTMESH_READ_TIMEOUT_MS:5000}
# Insecure lab-only default. Override it together with VAULT_API_TOKEN.
# For mTLS instead: set kfe.vaultmesh.tls.* (see kfe-service-vaultmesh-go-live.properties)
# and leave api-token empty — vault MutualTlsAuthAdapter refuses X-Vault-Token.
kfe.vaultmesh.api-token=${KFE_VAULTMESH_API_TOKEN:kerosene-vault-lab-only}
kfe.vaultmesh.tls.enabled=${KFE_VAULTMESH_TLS_ENABLED:false}
kfe.vaultmesh.tls.cert-path=${KFE_VAULTMESH_TLS_CERT_PATH:}
kfe.vaultmesh.tls.key-path=${KFE_VAULTMESH_TLS_KEY_PATH:}
kfe.vaultmesh.tls.ca-path=${KFE_VAULTMESH_TLS_CA_PATH:}
kfe.vaultmesh.tls.keystore-path=${KFE_VAULTMESH_TLS_KEYSTORE_PATH:}
kfe.vaultmesh.tls.keystore-password=${KFE_VAULTMESH_TLS_KEYSTORE_PASSWORD:}
kfe.vaultmesh.tls.truststore-path=${KFE_VAULTMESH_TLS_TRUSTSTORE_PATH:}
kfe.vaultmesh.tls.truststore-password=${KFE_VAULTMESH_TLS_TRUSTSTORE_PASSWORD:}
kfe.vaultmesh.tls.hostname-verification=${KFE_VAULTMESH_TLS_HOSTNAME_VERIFICATION:true}
